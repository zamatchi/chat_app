<div class="col-sm-8 offset-sm-1">
  <div class="mb-3">
    <p><span style="border-bottom: solid 1px;"><%= "#{@chatroom.name}の部屋" %></span></p>
  </div>
  
  <h3 class="text-center"><span style="border-bottom: solid 1px;">質問一覧</span></h3>

  <div>
    <%= render 'questions_list', questions: @questions %>
      <%= paginate @questions %>
  </div>
  <div class="text-right">
    <%= link_to "ルームへ戻る", chatroom_path(@chatroom), class: "btn btn-primary btn-sm" %>
    <%#= link_to "#{@chatroom.name}の部屋へ戻る", chatroom_path(@chatroom), class: "btn btn-primary btn-sm" %>
  </div>
</div>