<% if current_user == @user %>
  
  <% unless @bestanswer %>
    <%= form_with(model: question, local: true, method: :put) do |f| %>
      <%= f.hidden_field :bestanswer, :value => answer.id %>
  
      <%= f.submit "ベストアンサー", class: "btn btn-success btn-sm" %>
    <% end %>
  <% else %>
    <%= form_with(model: question, local: true, method: :put) do |f| %>
      <%= f.hidden_field :bestanswer, :value => nil %>
      <%= f.submit "ベストアンサーをはずす", class: "btn btn-danger btn-sm" %>
    <% end %>
  <% end %>
<% end %>